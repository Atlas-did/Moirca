# 分析过程视觉设计：像细胞生长一样的决策图谱

> 核心问题：节点和边以什么为标准？
> 答案：**以 Agent 的调研成果为节点，以发现之间的关联为边。**

---

## 一、本体定义：8 类节点 + 7 类边

### 节点类型（按"生长顺序"排列）

| 节点类型 | 图标/颜色 | 代表什么 | 谁创造的 | 生长时机 |
|----------|----------|---------|----------|---------|
| `user_profile` | ⭐ 金色 | 用户画像（分数+省份+选科+目标） | **种子** | 一开始就存在 |
| `school` | 🏫 翠绿 | 院校 | Agent 1 | 第一批生长 |
| `major` | 📘 蓝色 | 专业 | Agent 1 | 第一批生长 |
| `data_finding` | 📊 青色 | 具体数据发现（分数线/招生计划/排名） | Agent 1 | 第一批生长 |
| `sentiment` | 💬 紫色 | 口碑/情感分析摘要（含 kkdaxue 引用） | Agent 2 | 第二批生长 |
| `risk` | ⚠️ 红色 | 时效风险/专业撤销/招生变化 | Agent 3 | 第三批生长 |
| `conflict` | ⚡ 橙色 | 数据矛盾（官方 vs 口碑不一致） | Agent 4 | 第四批生长 |
| `advice` | 💡 琥珀 | 张雪峰建议/就业趋势/家庭适配建议 | Agent 5 | 第五批生长 |
| `fused` | 🔮 渐变 | 融合评分节点（Agent 6 综合打分） | Agent 6 | 第六批生长 |
| `recommend` | 🏆 金色 | 最终推荐（冲/稳/保标签） | Agent 6 | 最后一批 |

### 边类型

| 边类型 | 连接 | 含义 | 出现时机 |
|--------|------|------|---------|
| `offers` | school → major | 院校开设此专业 | Agent 1 |
| `scoreline` | user_profile → school/major | 分数与该校/该专业的关联 | Agent 1 |
| `has_sentiment` | sentiment → major | 这个专业有口碑数据 | Agent 2 |
| `has_risk` | risk → major/school | 这个专业/院校有风险 | Agent 3 |
| `conflicts_with` | data → data | 两个数据点互相矛盾 | Agent 4 |
| `advises` | advice → major | 张雪峰建议关联到专业 | Agent 5 |
| `scores` | fused → major | 融合评分关联到专业 | Agent 6 |
| `recommends` | recommend → major | 最终推荐关系 | Agent 6 |

---

## 二、生长过程动画（40 秒内完成）

### 第 0 秒：种子

Canvas 中央一个金色节点。旁边显示文字："正在为你分析..."

### 第 5-15 秒：第一批生长（Agent 1）

Agent 1 查询官方数据时，节点像细胞分裂一样从种子周围冒出来：

```
                    ⭐ 用户画像
                   /    |    \
        🏫华南理工  🏫深大  🏫广工
         /    \      /    \      /    \
      📘计算机 📘软工 📘计算机 📘软工 📘计算机 📘软工
       📊598分   📊585   📊572   📊565   📊548   📊540
```

动画效果：节点从种子位置"弹出"→沿弧形飞到最终位置→边随节点移动逐渐"拉出"→每个节点出现时轻微"脉冲"

### 第 15-22 秒：第二批生长（Agent 2）

口碑数据附着到专业节点上。紫色小节点像气泡一样从右侧放大出现。

```
            📘计算机 ←─── 💬 47条反馈·72%正向
```

### 第 22-28 秒：第三批生长（Agent 3）

红色风险节点附着到有问题专业上。

### 第 28-33 秒：第四批生长（Agent 4）

橙色矛盾边连接冲突的数据点，带闪烁效果。

```
      📊 就业率98%（官方） ⚡──────⚡ 📊 就业率65%（社区）
```

### 第 33-38 秒：第五批生长（Agent 5）

琥珀色建议节点出现，带张雪峰引语。

### 第 38-42 秒：第六批生长（Agent 6）

节点聚集成三个簇：🚀冲 / ✅稳 / 🛡保。最终决策图谱完成。

---

## 三、后端数据流（不改 Agent 内部代码）

### 核心思路

不改 Agent 1-6 的 `research()`，只在编排层 `_run_recommend_job` 每个阶段结束后构建 graph 数据写入 job。

```python
# 当前：
with _jobs_lock:
    _jobs[job_id]["progress"] = 30
    _jobs[job_id]["message"] = "Agent 1-5 并行调研..."

# 改成：
with _jobs_lock:
    _jobs[job_id]["progress"] = 30
    _jobs[job_id]["message"] = "Agent 1-5 并行调研..."
    _jobs[job_id]["graph"] = {
        "nodes": [当前所有已生成的节点],
        "edges": [当前所有已生成的边]
    }
```

### 每个阶段生成的图谱数据

```
阶段 0（种子）:
  nodes: [{id: "user", label: "585分·广东·物理", type: "user_profile", ...}]
  edges: []

阶段 1（Agent 1 完成）:
  nodes: 新增 school * N + major * N + data_finding * N
  edges: offers * N + scoreline * N

阶段 2（Agent 2 完成）:
  nodes: 新增 sentiment * N
  edges: has_sentiment * N

阶段 3（Agent 3 完成）:
  nodes: 新增 risk * N
  edges: has_risk * N

阶段 4（Agent 4 完成）:
  nodes: 新增 conflict * N
  edges: conflicts_with * N

阶段 5（Agent 5 完成）:
  nodes: 新增 advice * N
  edges: advises * N

阶段 6（Agent 6 完成）:
  nodes: 新增 fused * N + recommend * N
  edges: scores * N + recommends * N
```

### 复用现有的 KnowledgeGraph 组件

前端已有一个 400 行 Canvas 力导向图组件，支持：
- ✅ 物理引擎（斥力/弹力/向心力/阻尼）
- ✅ 拖拽交互
- ✅ Hover tooltip
- ✅ 不同类型节点（颜色/图标/半径不同）
- ✅ requestAnimationFrame 动画循环

改动量：
- **后端**：`_run_recommend_job` 每个阶段末尾构建 graph 数据
- **前端**：KnowledgeGraph 改为从轮询接口的 `graph` 字段读取，新节点出现时做"生长"动画
- **前端**：新增 8 种节点类型的 color/icon/radius 映射

---

## 四、为什么这个本体是对的

| 你的问题 | 回答 |
|---------|------|
| "节点以什么为标准" | **以 Agent 的调研产出为标准**。每个 Agent 完成→对应类型节点长出来 |
| "边以什么为标准" | **以数据之间的逻辑关联为标准**。分数线连接用户和学校，口碑连接专业，矛盾连接冲突数据 |
| "怎么像细胞生长" | 阶段性的、不可逆的、从少到多。先有用户→再有院校专业→再有口碑→再有风险→最终形成推荐簇 |
| "为什么不是知识图谱" | 知识图谱是静态的。**决策图谱是动态的**——每个节点代表一个分析发现，每根边代表一条推理链路 |
