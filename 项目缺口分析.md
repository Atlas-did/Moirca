# 砍功能：什么才是你的真实优势

> 所谓"铺面太大"，就是做了很多跟核心优势无关的事。

---

## 一、先盘点你手里到底有什么

### 后端：17 个 API 文件

| API | 代码量 | 服务于 | 对核心价值贡献 |
|-----|--------|--------|--------------|
| `recommendation.py` | 685 行 | **Agent 1-6 推荐管线** | 🔴 **核心** |
| `decision.py` | 186 行 | 决策树输入 | 🔴 **核心** |
| `chat.py` | 217 行 | 7 Agent 角色对话 | 🟡 酷但不核心 |
| `compare.py` | - | 两两志愿对比 | 🟢 有用但非必须 |
| `report.py` | 45 行 | 报告生成 | 🟢 有用 |
| `graph_data.py` | 241 行 | **知识图谱** | ⚪ 噪音 |
| `graph.py` | - | **知识图谱** | ⚪ 噪音 |
| `documents.py` | - | **文件上传构图** | ⚪ 噪音 |
| `interview.py` | - | Agent 采访 | ⚪ 噪音 |
| `logs.py` | - | 系统日志 | ⚪ 噪音 |
| `history.py` | - | 历史记录 | 🟡 有用但非必须 |
| `ontology.py` | - | **本体图谱** | ⚪ 噪音 |
| `scorelines.py` | - | 分数线管理 | 🟡 还没数据 |
| `simulation.py` | - | 模拟 | ⚪ 噪音 |
| `agents.py` | - | Agent 角色定义 | 🟡 边缘 |
| **14 个 API 里只有 2 个核心** | | | |

### 前端：94 个 tsx/ts 文件

| 面板 | 状态 | 对核心价值贡献 |
|------|------|--------------|
| ScoreInputBar | ✅ 已接真实 API | 🔴 **核心** |
| VolunteerTable | ✅ 已接真实 API | 🔴 **核心** |
| **KnowledgeGraph** | ✅ 已接 API | ⚪ **对用户零价值，砍** |
| **AgentChat** | ✅ 已接 API | 🟡 炫酷但不核心，藏 |
| **FullChat（6 Agent 辩论）** | ✅ 已接 API | 🟡 演示功能，藏 |
| **AHPMatrix** | ✅ 已接 API | ⚪ **用户不会用，砍** |
| **FileUploadPanel** | ✅ 已接 API | ⚪ **边缘场景，砍** |
| **SystemLogPanel** | ❌ 硬编码 | ⚪ **砍** |
| **DeepResearch** | ❌ 硬编码 | ⚪ **砍** |
| **ChartPanel** | ❌ 硬编码 | ⚪ **砍** |
| HistoryPanel | - | 🟡 有用但非必须 |
| ReportPanel | - | 🟢 有用 |
| GuidedReport | - | 🟢 有用 |

---

## 二、你的真实优势不是你以为的那些

### ❌ 不是优势的东西

| 你以为是优势 | 为什么不是 |
|-------------|-----------|
| "我有 7 Agent 系统" | 调 DeepSeek API 写几个 prompt 的事，门槛极低 |
| "我有知识图谱" | 用户看不懂也不关心 |
| "我有 AHP 对比" | 太学术，99% 的用户不会填矩阵 |
| "我有 Agent 辩论" | 很酷，但高考生不是来看辩论赛的 |
| "我有文件上传构图" | 边缘场景，不是核心需求 |

### ✅ 你真正的优势

| 真实优势 | 为什么是 | 竞品能抄吗 |
|---------|---------|-----------|
| **7 Agent 多视角推荐管线已跑通** | 不是"能跑"，是**已经跑通了**——输入分数到输出多视角分析，全链路通 | 能抄，但至少花 2 周 |
| **kkdaxue 10076 条已导入 Agent 2** | 数据在手，且**已经接进了 Agent 的推理管线** | 能爬但还没人做 |
| **张雪峰框架已融入 Agent 5 推理逻辑** | 不是"引用语录"，是**打分逻辑里嵌入了张雪峰的决策权重** | 能抄但需要理解框架 |
| **融合公式（Agent 6）已是成品** | 600 行的融合引擎，带 4 因子修正 | 能写但需要调参 |
| **前端已能展示推荐结果** | VolunteerTable + CardView 能用 | 抄 UI 容易 |

### 💎 一句话说出你的优势

> **Moirca 是目前唯一一个能把"分数 + 在校生真实体验 + 张雪峰决策框架 + 风险分析"组合成结构化决策简报的工具。**

不是因为技术多牛，而是**这几样东西正好都在你手里，而且已经接好了。**

---

## 三、聚焦之后应该长什么样

### 砍掉的面板：9 个

```
❌ 知识图谱（KnowledgeGraph）     → 对用户零价值
❌ AHP 矩阵（AHPMatrix）          → 用户不会用
❌ 文件上传（FileUploadPanel）    → 边缘场景
❌ 系统日志（SystemLogPanel）     → 运维工具
❌ 深度研究（DeepResearch）       → 伪需求
❌ 图表面板（ChartPanel）         → 等有用户数据再说
❌ 资源管理（ResourcesPanel）     → 内部工具
❌ Agent 采访（InterviewPanel）   → 演示功能
❌ Agent 辩论（FullChat）         → 隐藏入口，不删代码但不出现在主菜单
```

### 保留的面板：4 个

```
✅ 分数输入（ScoreInputBar）      → 首页唯一入口
✅ 推荐结果（VolunteerTable/CardView）→ 结果展示
✅ 对比分析（简化版）             → 推荐列表里勾选对比
✅ 报告/导出                     → 导出/分享转化
```

### 前端导航前后对比

```
现在（9 项）:         聚焦后（4 项）:
┌──────┐              ┌──────┐
│ 对话  │              │ 推荐  │ ← 首页即推荐
│ 志愿表│              │ 对比  │
│ 对比  │              │ 历史  │
│ 图谱  │              │ 设置  │
│ Agent │              └──────┘
│ 上传  │
│ 历史  │
│ 报告  │
│ 资源  │
└──────┘
```

### 用户使用路径

```
打开 app
  ↓
输入分数 + 省份 + 选科 + 毕业后打算（4 个输入，15 秒）
  ↓
等待 30-40 秒（异步不阻塞，显示进度）
  ↓
看到决策说明书（冲稳保卡片 + 每项含多视角分析 + 风险）
  ↓
可以：
  ├ 切换视角（就业/考研/考公/留学模式）
  ├ 勾选对比 2-3 个志愿
  ├ 导出为决策报告 PDF → 分享给家长/老师
  └ 收藏方案，下次回来对比不同方案

总共 4 个入口，1 条主路径。没有知识图谱、没有 Agent 聊天、没有矩阵。
```

---

## 四、后端保留 vs 隐藏 vs 删除

| 操作 | 文件 | 原因 |
|------|------|------|
| **保留** | `recommendation.py`, `decision.py` | 核心业务 |
| **保留** | `agents/` 全部 7 个 Agent | 核心引擎，一个不能少 |
| **保留** | `models/`, `services/model_router.py`, `services/privacy_guard.py` | 基础设施 |
| **保留** | `compare.py` | 对比是重要功能 |
| **隐藏** | `chat.py`, `agents.py` | 不删代码，不在主菜单暴露 |
| **不注册路由** | `graph_data.py`, `graph.py`, `documents.py` | 以后可能有用 |
| **不注册路由** | `interview.py`, `ontology.py`, `simulation.py`, `scorelines.py`, `zep.py` | 同上 |
| **合入核心** | `report.py` → 合入 `recommendation.py` | 报告是推荐的自然延伸 |

---

## 五、重新定义"做完了"的标准

| 之前以为"做完了" | 现在应该关注 |
|-----------------|-------------|
| 后端 17 个 API | 后端 3 个核心 API 做到 90 分 |
| 前端 10 个面板 | 前端 1 条主路径做到顺滑 |
| 爬 31 省分数线 | 不爬，用 LLM 公共知识 + 免责声明 |
| 搞 500 条规则 | 先搞 50 条核心里面的核心，验证有效再扩 |
| 做知识图谱 | 砍掉，用户用不上 |
| 做 Agent 对话 | 隐藏，不占主入口 |

---

## 六、聚焦之后你的竞争力反而更强

砍掉 9 个面板后，你的产品不是"变少了"，而是**变锋利了**。

```
砍之前：                  砍之后：
"一个有很多AI功能的        "一个能告诉你'这个专业
 高考志愿工具"             到底该不该报'的工具"

竞品说：                  你说：
"我有 20 万条数据"        "我有 7 个 Agent 从不同视角分析"
"我有 AI 填报"            "我有张雪峰框架 + 在校生真实反馈"
"我功能最全"              "我不要功能全，我要每个推荐都有道理"
```

掌上高考可以做 20 个功能，但他不会在每个推荐里写一段"张雪峰说 + 在校生说 + 风险提示"的组合分析。不是他不想，是他没有这几个东西串起来的管线。
