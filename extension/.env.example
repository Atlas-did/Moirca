# ============================================================
# WebBridge Chrome 扩展环境说明
# 扩展本身无运行时 .env:构建模式由 vite --mode 注入 __WEBBRIDGE_MODE__,
# 权限来自 manifest.pro.json / manifest.readonly.json。
# ============================================================

# ---- 构建矩阵(在 extension/ 目录执行) ----
# pro 模式(chrome.debugger 全权,通道④):
#   npm run build:pro                → 产物 dist-pro/
# readonly 模式(activeTab 最小权限,通道②):
#   npm run build:readonly           → 产物 dist-readonly/
# (npm run build = 两种模式一次构建)

# ---- 运行时后端地址 ----
# readonly popup 问答调 <backend>/api/context/ask,默认 http://localhost:8000;
# 如需修改,改 src/readonly/ask-client.ts 的 DEFAULT_BACKEND_BASE 后重新构建。

# ---- 与 daemon 配对 ----
# daemon 启动后把 daemon/.bridge-token 中的 64 位 hex 作为 hello 握手 token;
# 加载 dist-pro/ 后打开扩展 service worker 控制台查看连接状态(badge DBG)。
